# Original source data — keep local, never push.
*.xlsx
*.xls

# CSV imports/exports — trial data should never be tracked in git.
*.csv

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Local dev
node_modules/
.env
.env.local

# Secrets — never commit
*Password*
*password*
*secret*
*Secret*
